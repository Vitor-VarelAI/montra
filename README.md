# Montra

Internal Next.js tool for generating redesign demo previews.

## Local Development

```bash
cd app
npm install
npm run dev
```

The app runs on port `3010` by default.

## Repository Notes

Operational docs, prompt contracts, generated leads, and local context files are
kept out of Git intentionally. Configure runtime secrets locally through env
files; do not commit credentials or generated lead data.
